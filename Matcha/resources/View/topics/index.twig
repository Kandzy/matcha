{% extends 'layout/app.twig' %}

{% block content %}
    </br>
        {{ data.Login }}
    </br>
    {{ data.Password }}
    </br>
    {{ data.Email }}
    </br>
    {{ data.Notification }}
        <div class="topic">
            <h4 style="text-align: center">
                <a href="{{ path_for('topics.show', {id: topic.ID}) }}">{{ topic.getTitle }}</a>
            </h4>
        </div>
    {#{% endfor %}#}
{% endblock %}
{#{% extends 'layout/app.twig' %}#}

{#{% block content %}#}
    {#{% for topic in topics %}#}
        {#<div class="topic">#}
            {#<h4 style="text-align: center">#}
                {#<a href="{{ path_for('topics.show', {id: topic.ID}) }}">{{ topic.title }}</a>#}
            {#</h4>#}
        {#</div>#}
    {#{% endfor %}#}
{#{% endblock %}#}